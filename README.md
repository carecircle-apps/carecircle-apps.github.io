# CareCircle legal site (GitHub Pages)

This folder is the static site that serves CareCircle's Privacy Policy and Terms of Use at the URLs the app links to:

- https://carecircle-apps.github.io
- https://carecircle-apps.github.io/legal/privacy
- https://carecircle-apps.github.io/legal/terms

Those URLs are hard-coded in `app.json` under `expo.extra.privacyPolicyUrl` and `expo.extra.termsUrl`, and rendered as clickable links from the in-app Disclaimer screen and the Security → Legal card. If you change the URLs, you must rebuild the app.

The legal markdown lives in `legal/privacy.md` and `legal/terms.md` at the repo root. This site is a Jekyll-rendered copy generated by `scripts/sync-legal.mjs`. **Never edit `gh-pages-site/legal/*/index.md` by hand** — they get overwritten.

---

## First-time deploy (one-time, ~10 minutes)

### 1. Create the GitHub organization

The URL `carecircle-apps.github.io` requires a GitHub user OR organization named exactly `carecircle-app`. The cleanest setup is an org so multiple maintainers can contribute.

1. Go to https://github.com/account/organizations/new
2. Pick the **Free** plan
3. Organization account name: `carecircle-app`
4. Contact email: support@carecircle.app (or your personal email)
5. This organization belongs to: **My personal account**
6. Skip the "invite members" step (do this later)

### 2. Create the Pages repository

1. While signed in, go to https://github.com/organizations/carecircle-apps/repositories/new
2. Repository name: `carecircle-apps.github.io` (must match exactly)
3. Public (required for free GitHub Pages)
4. Do not initialize with a README — we have our own files to push
5. Click **Create repository**

### 3. Push the site contents

From the CareCircle repo root:

```sh
# Make sure the legal copies are up to date with the source markdown
npm run sync:legal

# One-off init for the Pages repo
cd gh-pages-site
git init -b main
git add .
git commit -m "Initial CareCircle legal site"
git remote add origin git@github.com:carecircle-apps/carecircle-apps.github.io.git
git push -u origin main
```

If you use HTTPS instead of SSH, replace the `git@github.com:` URL with `https://github.com/`.

### 4. Enable GitHub Pages

1. Go to the new repo's **Settings → Pages**
2. Source: **Deploy from a branch**
3. Branch: `main`, folder: `/` (root)
4. Save

GitHub will build the site on every push to `main`. The first build takes about a minute. When it's done, the **Settings → Pages** page shows a green badge with the URL.

### 5. Verify

Open each URL in a browser:

- https://carecircle-apps.github.io — landing page with links
- https://carecircle-apps.github.io/legal/privacy — Privacy Policy
- https://carecircle-apps.github.io/legal/terms — Terms of Use

These are the URLs the app links to. If any 404s, check the **Actions** tab on the Pages repo for build errors.

---

## Updating legal docs

Whenever you change `legal/privacy.md` or `legal/terms.md` at the CareCircle repo root:

```sh
# From the CareCircle repo root
npm run sync:legal

# Commit the source change + the regenerated copies together
git add legal/ gh-pages-site/legal/
git commit -m "Update Privacy Policy and Terms"
```

Then re-push the `gh-pages-site/` subtree to the Pages repo. The simplest pattern is to use `git subtree`:

```sh
git subtree push --prefix gh-pages-site \
  git@github.com:carecircle-apps/carecircle-apps.github.io.git main
```

You can also clone the Pages repo separately and copy the files over manually if subtree gives you trouble.

---

## Updating the document version

If you make **material changes** to either legal doc, bump `CURRENT_CONSENT_VERSION` in `src/services/legalConsent.ts`. The app re-prompts every existing user with the new disclaimer screen on next cold start.

---

## Local preview

The site is plain Jekyll, so you can preview it locally if you have Ruby installed:

```sh
cd gh-pages-site
bundle install
bundle exec jekyll serve
# → open http://127.0.0.1:4000
```

(Local preview is optional. GitHub Pages builds the site fine without ever running Jekyll on your machine.)

---

## Files in this folder

```
gh-pages-site/
  _config.yml              Jekyll configuration (theme, permalinks, etc.)
  _layouts/default.html    The shared HTML layout + inline CSS
  index.md                 Landing page (/)
  legal/
    privacy/index.md       Generated from legal/privacy.md
    terms/index.md         Generated from legal/terms.md
  Gemfile                  Optional, only for local previews
  .gitignore               Jekyll build artifacts
  README.md                You are here
```

Source-of-truth markdown lives in `legal/` at the CareCircle repo root.

---

*Adelante. 💛*
